[project]
name = "trading-engine"
version = "0.1.0"
description = "Paper trading engine for algorithm comparison"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
